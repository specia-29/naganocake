<div class="container">
  <div class="row">
    <div class="col-xl-4">
      <table class="table" >
        <thead>
          <tr><th>ジャンル検索</th></tr>
          </thead>
          <tbody>
          <% @genres.each do |genre| %>
            <tr><td><%= link_to genre.name,  %></td></tr>
          <% end %>
          </tbody>
      </table>
    </div>
  </div>
</div>