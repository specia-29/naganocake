<!--customers_withdrawal_path-->

<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <p class="check">本当に退会しますか？</p>
      <p class="introduction">
        退会すると、会員登録情報や<br>
        これまでの購入履歴が閲覧できなくなります。<br>
        退会する場合は、「退会する」をクリックしてください。
      </p>
      <%= link_to "退会しない", customers_mypage_path(@customer.id), class: 'btn btn-primary' %>
      <%= link_to "退会する", admin_root_path(@customer.id), class: 'btn btn-danger' %>
      <%#= form_with model: @customer, url: customers_mypage_path do |f| %>
      <%#= f.submit "退会する", class: 'btn btn-danger' %>
      <%# end %>
    </div>
  </div>
</div>