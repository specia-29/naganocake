<!--customers_withdrawal_path-->

<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <div class="mt-5 text-center">
      <p class="font-weight-bold">本当に退会しますか？</p>
      <p>
        退会すると、会員登録情報や<br>
        これまでの購入履歴が閲覧できなくなります。<br>
        退会する場合は、「退会する」をクリックしてください。
      </p>
      <%= link_to "退会しない", customers_mypage_path(@customer.id), class: 'mt-2 mr-3 btn btn-primary' %>
      <%= link_to "退会する", destroy_customer_path(@customer.id), method: :patch, "data-confirm" => "本当に退会しますか？", class: 'mt-2 btn btn-danger' %>
      </div>
    </div>
  </div>
</div>